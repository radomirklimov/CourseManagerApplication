<Window x:Class="KursVerwaltung.NewRoom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Room"
        Height="400" Width="700"
        Background="#F7F9FB"
        FontFamily="Segoe UI">
    <Border CornerRadius="12" Background="White" Padding="30" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Name:" FontWeight="Bold" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
            <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Padding="8"/>

            <TextBlock Text="Size:" FontWeight="Bold" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
            <TextBox x:Name="SizeTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Padding="8"/>

            <TextBlock Text="Max Capacity:" FontWeight="Bold" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
            <TextBox x:Name="MaxCapacityTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Padding="8"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,20,0,0">
                <Button Content="Add" Width="100" Margin="5" Click="Add_Click"
                        Background="#50C878" Foreground="White" FontWeight="Bold" BorderBrush="#50C878" Cursor="Hand"/>
                <Button Content="Cancel" Width="100" Margin="5" Click="Cancel_Click"
                        Background="#E74C3C" Foreground="White" FontWeight="Bold" BorderBrush="#E74C3C" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
